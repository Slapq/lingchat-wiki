# 🐱 LingPal 桌面宠物模式

LingPal 是 LingChat 的桌面陪伴形态，可在主窗口与悬浮桌宠之间切换，共享当前角色、对话与记忆。

## 使用顺序

1. 先启动 LingChat 主程序并确认大模型服务正常。
2. 再启动 LingPal，选择角色与桌宠尺寸。
3. 在桌宠菜单中开启主动对话、日程提醒或屏幕感知。

::: warning
桌宠依赖主程序提供服务。若先启动桌宠，可能出现连接失败；请先启动主程序后再启动桌宠。
:::

桌宠与网页端共享数据，出现立绘或台词不同步时可刷新网页或重启桌宠。屏幕感知默认关闭，启用前请确认隐私设置与视觉模型配置。
